# Normalize line endings: store text files with LF in the repo,
# and let Git check them out with the platform-native ending.
# Combined with prettier's "endOfLine": "auto", this prevents the
# "Delete `␍`" (CRLF) lint warnings on Windows.
* text=auto

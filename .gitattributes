# Force LF line endings in the repository AND in the working tree on every
# OS (overrides a local core.autocrlf=true on Windows). This keeps prettier's
# "endOfLine": "lf" happy everywhere and prevents the "Delete `␍`" warnings.
* text=auto eol=lf
